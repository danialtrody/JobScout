services:
  - type: web
    name: jobscout
    env: node
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: RENDER
        value: true
      - key: NODE_ENV
        value: production